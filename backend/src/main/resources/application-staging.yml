server:
  servlet:
    context-path: /attendancesystemstg/api

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/tuition_attendance_staging?createDatabaseIfNotExist=true&serverTimezone=UTC}

logging:
  level:
    com.tuition.attendance: INFO
