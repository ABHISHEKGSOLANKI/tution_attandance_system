server:
  servlet:
    context-path: /attendancesystem/api
spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/tuition_attendance_prod?createDatabaseIfNotExist=true&serverTimezone=UTC}

  jpa:
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    com.tuition.attendance: WARN
